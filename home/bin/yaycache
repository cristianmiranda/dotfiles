#!/bin/bash

yay_cache=`find $(yay -Pg | jq -r .buildDir) -maxdepth 1 -type d | awk '{ print "-c " $1 }' | tail -n +2`

echo -e "\n🧐 Looking for prunable uninstalled packages..."
paccache -ruvk0 $yay_cache
echo -e "\n🧐 Looking for prunable installed packages..."
paccache -rvk2 $yay_cache

