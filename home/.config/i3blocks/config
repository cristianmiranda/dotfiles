# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/lib/i3blocks/$BLOCK_NAME/$BLOCK_NAME
separator_block_width=25
markup=pango

[github]
command=$HOME/.config/i3blocks/scripts/github
interval=60
separator=false
separator_block_width=15

[thunderbird]
command=$HOME/.config/i3blocks/scripts/thunderbird
interval=5
separator=false
separator_block_width=15

[packages]
command=$HOME/.config/i3blocks/scripts/packages
interval=600

[microphone]
command=$HOME/.config/i3blocks/scripts/mic-status
interval=1

[airpods]
command=$HOME/bin/airpods
label=🎧 
interval=10

# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
[volume]
#label=VOL
label=🔊 
#instance=Master
#instance=PCM
interval=1
signal=10

[lg_vpn]
command=$HOME/.config/i3blocks/scripts/lg-vpn
interval=10

# CPU usage
#
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
[cpu_usage]
label=⚙️ 
interval=10
min_width=⚙️ 99.99%

# Memory usage
#
# The type defaults to "mem" if the instance is not specified.
[memory]
label=M 
separator=true
interval=30

#[memory]
#label=SW 
#instance=swap
#separator=false
#interval=30

#[bandwidth]
#instance=eth0
#interval=5

[nm-vpn]
#label=VPN:
command=$HOME/.config/i3blocks/scripts/nm-vpn.sh
interval=5

#[load_average]
#interval=10

[battery]
command=$HOME/.config/i3blocks/scripts/battery
interval=5

[weather]
command=curl "wttr.in/?format=1"
interval=1800

# Date Time
#
[time_nyc]
command=echo 🗽 $(TZ=":America/New_York" date +%H:%M:%S)
interval=1

[time]
command=echo $(echo 📅 $(date '+%d/%m/%Y') 🕑 $(date '+%H:%M:%S'))
interval=1

[dunstctl]
command=$HOME/.config/i3blocks/scripts/dunstctl
interval=5
separator=false
separator_block_width=15

[start]
command=echo ''
interval=7200
separator=false
